[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/dist"

# If you need environment variables, set them in Netlify UI:
# e.g. VITE_API_BASE_URL = https://your-backend.example.com

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

